//CONTİNUE

#include<stdio.h>
int main(){
	
int x;

for(x = 1; x <= 10; x++){
	
	if(x == 5){
		continue;			//continue skips to end of for lopp and performs next iteration.
	}
	printf("%d", x);
}	
	printf("\nUsed contineu to skip printing the value 5\n");
	
	return 0;
}
