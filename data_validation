#include <stdio.h>

int main (void)
{
	double num1,num2;
printf("Enter two numbers: ");
scanf("%lf %lf",&num1,&num2);

while(num2==0){
	printf("\n division by zero is not allowed! please reenter the second number: ");
	scanf("%lf",&num2);
}
	printf("result is %.2f\n",num1/num2);
}
