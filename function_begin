#include<stdio.h>

char fun(){
	return 'a';
} 

int main(){
	
char c = fun();
printf("Character is: %c", c);
	
}


/*
#include<stdio.h>

int add(int, int);

int main(){
	
	int m = 20, n = 30, sum;
	sum = add(m,n);
	printf("Sum is %d", sum);
return 0; 
}

int add(int a, int b){
	return(a+b);
}

*/

/*
#include<stdio.h>

double meters_to_miles(double meters){
	double miles;
	miles = meters / 1609.344;
	return(miles);
}
int main(void){
	double meters;
	
	printf("Enter a number as fallows meters: ");
	scanf("%lf", &meters);
	
	printf("%f meters equal to %f miles",meters, meters_to_miles(meters));
}
*/	




/*#include<stdio.h>
#include<stdlib.h>

int fact(int n){

int k, factorial = 1;
	
	for(k = 1; k <= n; k++){
		factorial *= k;
	}
	
	return(factorial);
}

int main(){
	
	int n;
	
	printf("Enter a number: ");
	scanf("%d", &n);
	
	printf("Factorial of n is: %d ", fact(n));
	
}
*/
