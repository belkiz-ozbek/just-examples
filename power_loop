#include <stdio.h>
#include <math.h>
int main (void){

int i,n;
double pow=1.0;

printf("enter n: ");
scanf("%d",&n);

if(n<=0){
	for(i=1; i<=-1*n; i++)
		pow = pow*1/2.0;	
}else{
	for(i=1; i<=n; i++){
		pow *=  2.0;
	}
}

printf("\n2 to the power %d is %f\n ",n,pow);

}
