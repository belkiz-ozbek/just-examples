#include<stdio.h>

int square(int y);  //function prototype

int main(){
	
	int x; //counter
	
	for(x = 1; x <= 10; x++){
		printf("%d", square(x));
		printf("\n");
	}

	return 0;
}

// Square funct,on definition returns square of parameter

int square(int y){
	
	return y*y;
	
	}





